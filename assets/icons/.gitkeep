# Keeps the assets/icons/ directory in version control.
# Flutter requires directories declared under `flutter: assets:` in pubspec.yaml
# to exist at build time. Place app icons (SVG rasterized / PNG) here.
