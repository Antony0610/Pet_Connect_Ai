# Keeps the assets/illustrations/ directory in version control.
# Flutter requires directories declared under `flutter: assets:` in pubspec.yaml
# to exist at build time. Place empty-state / onboarding art here.
